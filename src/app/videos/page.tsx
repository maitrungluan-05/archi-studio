import type { Metadata } from "next"; import { Play } from "lucide-react"; import { AssetCard } from "@/components/asset-card"; import { getMockAssets } from "@/lib/mock-data";
export const metadata:Metadata={title:"Film Archive",description:"Moving-image records from ARCHI."};
export default function VideosPage(){const assets=getMockAssets().filter(a=>a.type==="VIDEO");return <main className="archive-dark page-stage bg-natural-black text-white"><header className="page-mast border-white/15"><div className="container-wide page-mast__grid"><div><p className="page-kicker">Moving image / Screening room</p><h1 className="page-title !text-white">Time in motion.</h1></div><p className="page-deck !text-white/60">Films, studies, and visual sequences. Every record preserves the technical and authorship context around the frame.</p></div></header><section className="container-wide py-16 md:py-24">{assets.length?<div className="space-y-12">{assets.map((a,i)=><div key={a.id} className={`relative ${i%2?"ml-auto max-w-4xl":"max-w-5xl"}`}><span className="absolute left-5 top-5 z-20 flex h-14 w-14 items-center justify-center rounded-full border border-white/40 bg-black/30 backdrop-blur"><Play size={18} fill="white"/></span><AssetCard asset={a} variant="wide" className="bg-transparent [&_h3]:!text-white [&_p]:!text-white/50 [&_.archive-tile__caption]:border-white/20"/></div>)}</div>:<div className="research-empty border-white/20 bg-white/5"><div><Play className="mx-auto mb-4"/><h2 className="font-editorial text-3xl !text-white">The screening room is between programs.</h2></div></div>}</section></main>}
